name: refresh_interceptor
description: Reusable Dio interceptor for attaching access tokens, refreshing them once, and retrying failed requests.
version: 0.2.0
repository: https://github.com/pinzarulab/refresh_interceptor

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  dio: ^5.7.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.8
